<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EasyDo" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bluegrey900</item>
        <item name="colorPrimaryVariant">@color/bluegrey900</item>
        <item name="colorOnPrimary">@color/bluegrey600</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/bluegrey900</item>
        <item name="colorSecondaryVariant">@color/bluegrey900</item>
        <item name="colorOnSecondary">@color/bluegrey600</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textColorHint">@color/textColor</item>

        <!-- Status bar color. -->

        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.EasyDo.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.EasyDO.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.EasyDO.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- Dark theme -->

</resources>